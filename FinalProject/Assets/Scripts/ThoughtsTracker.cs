using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ThoughtsTracker : MonoBehaviour
{

    // Start is called before the first frame update
    void Start()
    {
        var scenegetter = GetComponent<UnityEngine.Video.VideoPlayer>();

    }

    // Update is called once per frame
    void Update()
    {
       // if (scenegetter.clip = Resources.Load<UnityEngine.Video.VideoClip>("Cartoon1")){
           // Debug.Log("money");

       // }
    }
}
